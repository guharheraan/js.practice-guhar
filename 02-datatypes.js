'use strict'; //to be strict in your code your code will be treated as newer version of JSON.

let vari = 42; // its a number

let ghk = 'student'; //its a string

let boolVar = true; //boolean variable, it can have only two values:true or false

console.table(typeof [vari, ghk, boolVar]); //this function is used to print the array elements in table
console.log(typeof vari); // this function returns the type of data that is stored inside the variable
console.log(typeof ghk);
console.log(boolVar); //we can assign value to boolean variable but we should not give any other than true or false

let num = '15';

let result = num + 10; //in such a case the javascript converts the number to a string and the  performs the string concatination.

// console.log(result);
// console.log(typeof result);

//number has the rang of (2^53 -1)

//bigint is used when we need to calculate a numbers greater than 15 digits like > 9,007,199,254,740,991

//string=> "guhar is a student"

//boolean => true or false

// undefined    when the variavles has not been defined then

//null => is a stand alone value and it returns as object

console.log(typeof null);

console.log(typeof undefined);
